export enum CharNamesE { // TODO: Prolong this list in the future with other char names
    ROBOT = 'robot',
}

export enum CharStatusesE {
    IDLE = 'idle',
    MOVE = 'move',
    MELEE = 'melee',
    RANGE = 'range',
    HURT = 'hurt',
    DIE = 'die',
    DISABLE = 'disable',
    ENABLE = 'enable',
    OFF = 'off',
    DEAD = 'dead',
}
